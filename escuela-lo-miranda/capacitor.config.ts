import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'escuela-lo-miranda',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
